# 参与开发

“枕边造梦”面向非技术用户。功能正确性、隐私边界、操作可理解性和失败反馈同等重要。

## 提交要求

- 用户可见功能变化必须更新 `CHANGELOG.md`。
- 当前功能、平台要求或命令发生变化时必须更新 `README.md`。
- 页面名、按钮名、输入限制和错误提示应与中文界面一致。
- 不要在功能改动中夹带无关重构或生成文件。

## 本地验证

```bash
npm ci
npm run typecheck
npm test
npm run build
```

官网改动还应执行：

```bash
cd website
npm ci
npm run build
```

安装包改动应在目标操作系统执行对应打包命令，并检查生成文件名、架构和启动行为。

## 安全与隐私

禁止提交：

- MiniMax、Cloudflare R2 或其他服务的 API Key 和 Token；
- 用户录音、儿童身份信息、未公开故事和导出成品；
- `.env`、`.env.release.local`、依赖缓存、构建目录和安装包；
- 代码签名证书、私钥和其他个人凭据。

提交前检查 `git status` 和待提交差异，确保仓库只包含预期源码、配置模板与有权公开的演示素材。
