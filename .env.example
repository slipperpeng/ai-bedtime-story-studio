# Optional development-only environment import. The packaged app stores the key
# through Electron safeStorage instead of reading this file.
BEDTIME_IMPORT_PROVIDER_KEYS=0
MINIMAX_API_KEY=
MINIMAX_API_ORIGIN=https://api.minimaxi.com

# Live integration tests are disabled unless explicitly enabled.
RUN_MINIMAX_LIVE=0
RUN_FULL_LIVE=0
