// The online music-generation provider remains paused. Background music now uses
// the offline library packaged with the application and does not consume API quota.
export const BACKGROUND_MUSIC_FEATURE_ENABLED = true
