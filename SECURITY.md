# 安全与隐私

“枕边造梦”会处理成年人授权声音样本、儿童故事设定、生成内容和第三方 API 凭据。不要在公开 Issue、讨论、截图或日志中提交真实录音、儿童个人信息、API Key、Token、加密凭据文件或完整本地工作区。

## 报告安全问题

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密入口。不要先创建公开 Issue。

报告应包含：

- 应用版本和操作系统；
- 可复现的最小步骤；
- 预期行为与实际行为；
- 已移除密钥、录音、儿童信息和本机绝对路径的必要日志；
- 可能的影响范围。

## 重点范围

- API Key 明文泄露或被发送到非预期服务；
- 原始声音样本、音色绑定或儿童数据越权读取；
- 导出 HTML 意外包含原始录音、密钥、草稿或内部路径；
- Electron 导航、IPC、资源协议或本地存储权限绕过；
- 路径穿越、任意文件读写或非预期网络访问；
- 未授权声音复刻或绕过成年人和授权确认。

## 使用建议

- 只使用本人或已明确授权成年人的声音；
- 儿童信息尽量只使用昵称；
- 仅从可信发布地址下载安装包，并核验发布校验值；
- 分享 HTML 前完整检查内容，并告知接收者其中包含 AI 合成朗读；
- 不再使用的 API Key 应在服务商控制台撤销或轮换。
